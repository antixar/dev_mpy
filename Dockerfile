# Create a image Dockerfile for the next container.
# This container has to check the availability of Google from a person PC and return this status as code error.
# You are able to use shell and docker tools for this only.
